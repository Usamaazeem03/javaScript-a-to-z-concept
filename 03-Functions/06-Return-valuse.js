// Return Valuse
function add(a, b) {
  return a + b;
}
const result = add(5, 3);
console.log(result); // Output: 8

// Summary ✨
// This code demonstrates the use of return values in JavaScript functions. The add function takes two parameters, a and b, and returns their sum using the return statement.
// The returned value is then stored in the variable result and printed to the console.
