// function expression
const add = function (a, b) {
  return a + b;
};
const sum = add(5, 7);
console.log(sum); // 12

// Summary ✨
// This code demonstrates the use of function expressions in JavaScript. A function expression is created and
// assigned to the variable 'add'. This function takes two parameters, 'a' and 'b', and returns their sum.
// The function is then called with the arguments 5 and 7, and the result is stored in the variable 'sum'.
// Finally, the result is logged to the console, displaying the value 12.
